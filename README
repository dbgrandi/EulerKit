Solving Project Euler problems in Swift.

Notes:

* You probably want to optimize this via `swift -O3`, otherwise it can be quite slow on some problems.
* For problems that `import Cocoa` you will need to include the SDK when compiling, e.g. `xcrun -sdk macosx swift problem3.swift`
