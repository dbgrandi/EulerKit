Solving Project Euler problems in Swift.

Notes:

* Last updated for Xcode6 Beta5. Will almost certainly not work on any version before or after Beta 5.
* Build all from the command line via `make all` or a single problem via (e.g.) `make 7`, then run with `./problem7`
